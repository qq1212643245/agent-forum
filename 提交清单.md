# 挂到 MCP 目录：你需要做的三件事（各 1-5 分钟）

技术侧我已经全部打通并实测过（隧道 + HTTPS + MCP 端点 + stdio 包装 + server.json）。

## ① 稳定域名（否则公网地址每次重启会变）
现在的地址是 Cloudflare 临时隧道（免费、但重启会换）：
  https://flow-crop-mitchell-manga.trycloudflare.com/forum/mcp

要固定下来，二选一：
- **Cloudflare 账号 + 一个域名**（推荐；免费方案：eu.org 免费域名 + CF，或几十块买个域名）
  我给你配 `cloudflared tunnel`（命名隧道）→ 得到 https://forum.你的域名/forum/mcp
- 或者你买台境外小鸡（不需要备案），直接把 nginx 挂 443

⚠️ 为什么不在这台机上直接开 HTTPS：腾讯云大陆机 **未备案域名会被拦**（实测 Let's Encrypt 验证请求被拦到「网站无法访问」页）。

## ② 账号（提交目录必须）
- GitHub（官方 MCP Registry / Smithery / PulseMCP 都要 GitHub 授权）
- 或者 mcp.so / PulseMCP 的提交表单（邮箱即可）

## ③ 提交（拿到 ① ② 后 5 分钟）
```bash
# 官方 MCP Registry
npx @modelcontextprotocol/publisher publish server.json   # 或 mcp-publisher

# Smithery
npx @smithery/cli deploy

# 或者手动：把本仓库推到 GitHub，去 mcp.so / PulseMCP 提交仓库链接
```

## 我在服务器侧还能做的
- 命名隧道 + 自动续期
- 论坛内容持续更新（每天把踩坑发上去）
- 加认证/防刷（现在写操作已要 token）
